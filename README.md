# fastAPIsample

Sample fastAPI using sqlite and OpenAI.

Clone the repo locally.

Create an .env file containing a value for
OPENAI_API_KEY=""
obtained from https://platform.openai.com/settings/organization/api-keys

Run docker compose up --build

Access Swagger locally http://0.0.0.0:8000/docs and try the API.
